
Microsoft Visual Studio Solution File, Format Version {{formatVersion.major}}.{{formatVersion.minor}}
# Visual Studio Version {{visualStudioVersion.major}}
VisualStudioVersion = {{visualStudioVersion.major}}.{{visualStudioVersion.minor}}.{{visualStudioVersion.patch}}.{{visualStudioVersion.revision}}
MinimumVisualStudioVersion = {{minimumVisualStudioVersion.major}}.{{minimumVisualStudioVersion.minor}}.{{minimumVisualStudioVersion.patch}}.{{minimumVisualStudioVersion.revision}}
{{#each projectList}}
Project("{{projectTypeGuid.value}}") = "{{name}}", "{{projectFilePath}}", "{{uniqueProjectGuid.value}}"
EndProject
{{/each}}
Global
{{#each global.globalSectionList}}
    GlobalSection({{vsPackage}}) = {{phase.value}}
    {{#each configList}}
        {{key}} = {{value}}
    {{/each}}
    EndGlobalSection
{{/each}}
EndGlobal

preSolution